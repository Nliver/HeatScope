# UI 文案分级迁移记录

本记录对应 L1/L2/L3 文案模型与 R1-R8。L1 操作、状态和关键数据保留常驻；以下条目均为从正文移出的 L3，或按 R1/R3/R6 收敛的重复与空状态文案。

> 2026-08 决议：上传格式和必需字段属于操作前置 L2，必须可见；Prompt、源码、原始报错、模型 JSON 和调试输出在专用工作区属于当前任务内容，直接展示并使用内部滚动。非必要 L3 可以省略，不为“信息保留”强制增加 Tooltip。
>
> 2026-08-01 最新覆盖：Step 3 的生成页面预览、HTML 源码和本次生成 Prompt 改为默认折叠，点击标题后在原容器内展开；Provider 原始报错、原始模型 JSON 和调试输出仍直接展示。演示案例恢复为普通导入卡，摘要保持卡内可见。

## 文案迁移表

| 原文案 / 原语义 | 级别 | 新承载方式 | 新位置 | 规则 |
|---|---|---|---|---|
| 配置并测试供应商模型后，诊断任务会读取已启用且连接成功的服务。 | L3 | `ⓘ` | 模型配置页标题旁 | R2, R7, R8 |
| 只有启用且连接成功的模型才会参与分析和 HTML 生成。 | L3 | `ⓘ` | 已配置服务标题旁 | R2, R7, R8 |
| 密钥只会随分析请求通过 HTTPS 发送，当前版本保存在浏览器工作区。 | L3 | `ⓘ` | 模型配置操作区 | R2, R8 |
| 每次分析会先跑本地规则，再并行调用模型；选中结果后，可继续生成独立 UI。 | L3 | `ⓘ` | 模型服务弹窗标题区 | R2, R8 |
| 一次完整流程的模型调用与热力图 token 消耗说明。 | L3 | 不常驻、不加 Tooltip | 需要时由模型任务计数直接表达 | R2, R7 |
| 添加后请先测试连接，再参与分析。 | L3 | `ⓘ` | 添加模型服务标题旁 | R2, R7, R8 |
| 上传文件的格式和字段要求。 | L2 / 操作前置 | 正文常驻 | 各上传控件名称下方 | R7, R8 |
| 模型只会基于 URL、热力图、点击数据、受众和品牌变量生成诊断与 UI。 | L3 | `ⓘ` | Step 1 标题旁 | R2, R8 |
| 演示案例摘要和热力图信息。 | L2 | 卡内短段落 | 各演示案例导入卡 | R2, R7 |
| 本地规则与多模型并行、勾选 finding、合成 Prompt 和生成任务的流程说明。 | L3 | 不常驻、不加 Tooltip | 由模型进度、选择和 Job 状态直接表达 | R1, R2, R7 |
| 模型分析任务的连接、消耗与本地规则兜底说明。 | L3 | 不常驻、不加 Tooltip | 由连接状态和任务状态直接表达 | R2, R7 |
| 抓取原站结构后，UI 方案会沿用站点语境。 | L3 | 不常驻、不加 Tooltip | 页面结构基线结果直接表达 | R1, R2, R7 |
| 点击、矩形和圆形工具会把热力图坐标同步到分析结果。 | L3 | `ⓘ` | 证据工作台标题旁 | R2, R7, R8 |
| 上传截图后可在图上定位元素并与分析联动。 | L3 | `ⓘ` | 热力图空状态 | R3, R8 |
| 左侧对比分析源、右侧查看反馈并勾选模型生成 HTML。 | L3 | `ⓘ` | 模型分析标题旁 | R2, R7, R8 |
| 意图、视觉输入、坐标和本地硬约束的 Prompt 合成机制。 | L3 | 不常驻、不加 Tooltip | 由实时 Prompt 内容直接表达 | R2, R7 |
| 绿色片段由模型选择、视觉开关、坐标与本地命中动态生成。 | L3 | 不常驻、不加 Tooltip | 绿色高亮本身承担状态表达 | R2, R7 |
| 合成 Prompt 全文。 | L1 / 当前工作内容 | 直接显示、区域内滚动 | Prompt 合成器实时预览 | R7, R8 |
| 本次生成 Prompt 全文。 | L1 / 调试结果 | 默认折叠，展开后区域内滚动 | HTML 结果 Prompt 区 | R5, R7, R8 |
| 生成页面预览。 | L1 / 结果查看 | 默认折叠，展开后在原容器内预览 | Step 3 HTML 结果区 | R5, R7, R8 |
| 该模型返回的 HTML 源码。 | L1 / 调试结果 | 默认折叠，展开后区域内滚动 | Step 3 HTML 结果区 | R5, R7, R8 |
| 任一模型成功即可进入第 3 步、失败项在第 3 步重试的生成机制。 | L2 | 一行短提示 | Prompt 合成器操作条 | R2 |
| 每个模型 HTML 独立保存、错误与重试在结果页处理。 | L3 | `ⓘ` | Step 3 / HTML 结果标题旁 | R1, R2, R8 |
| 生成页面随证据变化、并非固定模板的机制说明。 | L3 | `ⓘ`，重复语义合并 | 生成逻辑 / 证据适配逻辑标题旁 | R1, R2, R8 |
| 优化点清单随模型切换并保留本地硬约束。 | L3 | `ⓘ` | 优化点清单标题旁 | R2, R8 |
| evidence_ref 校验与拒收机制。 | L3 | 不常驻、不加 Tooltip | 由证据链结果和错误状态表达 | R2, R7 |
| 复盘需比较版本、设备、时间窗口与目标事件。 | L3 | `ⓘ` | Step 4 标题旁 | R2, R8 |
| 点击量不代表转化提升，需补充版本、曝光、结果事件和实验分流。 | L3 | `ⓘ`，因果限制仅由底部 caveat 常驻 | 复盘结果区 | R1, R2, R8 |
| 母版案例已载入；启用原则会注入模型并约束诊断口径。 | L3 | 不常驻、不加 Tooltip | 由启用状态和实际任务结果表达 | R2, R7 |
| 规则库适用页面、目标或业务范围。 | L2 / 业务说明 | 单行摘要 | 当前规则库标题下方 | R2 |
| 导入案例后选择已连接模型、预览并确认写入的流程。 | L3 | `ⓘ` | AI 归纳入口 | R2, R7, R8 |
| API Key 只随归纳请求发送，不进入知识库或导出。 | L3 | `ⓘ` | AI 归纳控件标题旁 | R1, R2, R8 |
| 保存后可继续新增和编辑具体规则。 | L3 | `ⓘ` | 规则库编辑弹窗操作区 | R2, R8 |
| 请从当前规则库选择具体规则查看完整说明。 | L3 | `ⓘ` | 当前条目空状态 | R3, R8 |
| 快照写入后冻结，查看和继续复盘不进入可编辑 Wizard。 | L3 | 不常驻、不加 Tooltip | 由详情页只读状态和可用操作表达 | R2, R7 |
| 完成一次分析后会在此保存不可变快照。 | L3 | 移除重复机制说明，保留短状态 | 历史列表空状态“暂无历史快照” | R1, R3 |
| 旧版本未保存完整输入与输出，不回退 Wizard 补写。 | L3 | `ⓘ` | “旧版本迁移记录”状态旁 | R2, R8 |
| 保存复盘前后校验 checksum，诊断字段禁止修改。 | L3 | `ⓘ` | 复盘提交操作旁 | R2, R8 |
| 历史与当前模型配置只比较指纹、不展示明文 API Key。 | L3 | `ⓘ`，两张对比卡的重复说明合并 | 复用历史配置弹窗标题区 | R1, R2, R8 |
| 模型密钥仅在分析请求中使用，不写入项目文件。 | L3 | `ⓘ` | 全局侧栏“当前工作区” | R1, R2, R8 |

## L2 与格式收敛

| 改造项 | 结果 | 规则 |
|---|---|---|
| “证据包已确认”类重复横幅 | 删除重复常驻提示，由 Stepper 状态和操作结果承担 | R1, R4 |
| “页面结构基线”重复说明 | 仅保留一个区块标题，其余说明进入 `ⓘ` | R1, R2 |
| “（可选）/（可编辑）”括号注释 | 改为共享次要标签 `.field-optional` | R6 |
| 模型、热力图、规则、历史、UI 结果空状态 | 收敛为“待分析 / 未抓取 / 待选择元素 / 暂无历史快照 / 未生成 UI 蓝图”等单行短语 | R3 |
| Placeholder | 仅保留输入示例或格式，不承载功能解释；可见 label 全部保留 | R7, R8 |

## DoD 自检

- [x] 无已知重复确认横幅；共享状态、步骤条和计数仍为唯一常驻状态源。
- [x] 每个已治理 section 最多一个标题，L3 不再作为副标题或正文常驻。
- [x] 非必要 L3 不再为了“信息保留”强制增加 Tooltip；必要内容由状态、结果或共享 `InfoHint` 承载。
- [x] 空状态已压缩为单行短语；存在可执行动作时保留原操作按钮。
- [x] 合成 Prompt 实时显示；Step 3 生成页面预览、HTML 源码和本次生成 Prompt 默认折叠，展开后在专用工作区内部滚动；原始报错保持直接可见。
- [x] UI 中无“（可选）/（选填）”括号注释。
- [x] `InfoHint` 使用可聚焦按钮、ARIA 关联，支持点击打开、外部点击或 Escape 关闭，并提供统一 placement。
- [x] L1 按钮、字段标签、状态、计数、错误原因和业务结果未删除。
