{
  "name": "heatscope",
  "version": "0.5.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "deploy": "vercel --prod --cwd apps/web"
  },
  "devDependencies": {
    "vercel": "^56.5.0"
  }
}
